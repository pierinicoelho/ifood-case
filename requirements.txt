# ── Databricks (instalar manualmente no cluster) ──────────────────────────────
# PySpark e Delta Lake são fornecidos pelo runtime — não requerem instalação.
matplotlib  # notebooks de análise (visualizações)
requests    # src/raw/ingest_raw.py — download dos Parquets TLC

# ── Execução local (fora do Databricks) ───────────────────────────────────────
# pyspark>=3.4.0
# delta-spark>=2.4.0   # versão deve coincidir com a do pyspark
# pandas               # dependência implícita do .toPandas()
# pyarrow              # leitura de Parquet
